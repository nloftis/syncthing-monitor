services:
  syncthing-monitor:
    image: python:3.13-alpine
    container_name: syncthing-monitor
    user: "1027:100" # svc-docker / users
    environment:
      TZ: Pacific/Honolulu
    volumes:
      - ./syncthing-monitor.py:/app/syncthing-monitor.py:ro
      - ./state:/state
    # Take the variables in .env and put them into the container's environment.
    env_file:
      - .env
    working_dir: /app # cwd
    command:
      - python
      - -u
      - /app/syncthing-monitor.py
    network_mode: host
    restart: unless-stopped
